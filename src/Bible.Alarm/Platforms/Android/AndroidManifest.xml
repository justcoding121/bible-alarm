<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.WAKE_LOCK" />
	<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
	<uses-permission android:name="android.permission.VIBRATE" />
	<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
	<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
	<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
	<uses-permission android:name="android.permission.REORDER_TASKS" />
	<!-- Permission to access notification policy for Do Not Disturb bypass -->
	<uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
	<!-- Remove deprecated permission if it exists in merged manifest -->
	<uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" tools:node="remove" />
	<!-- Remove advertising ID so we do not have to declare it in Play Console (app does not use ads) -->
	<uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
	
	<!-- allowBackup=false: app stores schedule/notification data; backup could expose it (Sonar S6358) -->
	<application android:allowBackup="false" android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:label="Bible Alarm" android:usesCleartextTraffic="false">
		<!-- CRITICAL: Application-level legacy metadata for Android Auto discovery -->
		<!-- This is required for Android Auto to discover and list the app -->
		<!-- Spotify uses this approach - application-level metadata for discovery -->
		<meta-data
			android:name="com.google.android.gms.car.application"
			android:resource="@xml/automotive_app_desc" />
	</application>
</manifest>
