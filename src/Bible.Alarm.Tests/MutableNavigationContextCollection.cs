namespace Bible.Alarm.Tests;

/// <summary>
/// Serializes tests that mutate static navigation context (shared mutable state).
/// </summary>
[CollectionDefinition(nameof(MutableNavigationContextCollection), DisableParallelization = true)]
public sealed class MutableNavigationContextCollection;
