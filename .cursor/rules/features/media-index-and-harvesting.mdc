---
description: "Media index (mediaIndex.db): how it’s harvested, seeded, and used for ad-hoc downloads"
globs: "{.tools/Bible.Alarm.AudioLinksHarvestor/**,src/Bible.Alarm/Services/Media/**,src/Bible.Alarm.Shared/Services/Media/**,src/Bible.Alarm.Shared/Models/Media/**,src/Bible.Alarm.Shared/Models/Enums/HarvestType.cs}"
alwaysApply: false
---

## What “the media index” is

Bible Alarm ships with a **SQLite “media index” database** (`mediaIndex.db`) that contains:
- languages (JW language codes + direction)
- categories (Bible/Music/Dramas/Video)
- publications + (optional) sections + tracks
- URL construction parameters for JW CDN APIs
- “discovery tables” that list which languages/sections exist for a publication

At runtime, the app:
- reads the index to populate pickers and resolve “what should play next”
- **downloads audio files** into cache on demand (or prefetch)
- may **ad-hoc harvest** a publication in a non-seeded language using the same JW APIs the harvester uses

---

## Media DB “shape matrix” (publication / optional language / optional sections)

## Categories seeded in harvester

Canonical list: **CategorySeeder** (`.tools/Bible.Alarm.AudioLinksHarvestor/Seeders/CategorySeeder.cs`) seeds category codes only; display names are in shared project JSON (`Resources/CategoryNames/E.json`). **JwSourceHelper** (`src/Bible.Alarm.Shared/Helpers/JwSourceHelper.cs`) maps category → publication codes; **PublicationTypeHelper** (`src/Bible.Alarm.Shared/Helpers/PublicationTypeHelper.cs`) drives harvest type and section structure.

**Publication lists**: The single source of truth for which publications we fetch is **JwSourceHelper**. The **Drama harvester** iterates **JwSourceHelper.AllMediatorPublicationCodes** (all mediator categories). **Video harvester** iterates **VideoPublicationCodes** and **SeriesPublicationCodes** (flat/index-service API). Bible and Music harvesters use **BiblePublicationCodes**, **VocalMusicPublicationCodes**, **MelodyMusicPublicationCodes**. Before Phase 2, the harvester runs **HarvestValidator.ValidateMediatorLinksAsync** (GET each `/categories/E/{code}`, assert `category.media` array); failure exits with code 1.

Base URL for GETPUBMEDIALINKS: `https://b.jw-cdn.org/apis/pub-media/GETPUBMEDIALINKS`. Mediator discovery: `https://app.jw-cdn.org/apis/mediator/v1/categories/{lang}/{categoryKey}`.

### Category summary table (all publications by category)

| Category (code, PascalCase) | Publication codes | Harvest type | Language-bound |
|-----------------------------|-------------------|--------------|----------------|
| Bible | nwt, bi12 | Sectioned | Yes |
| Music | osg, sjjc, sjji, snv, pksjj (vocal); iam (melody) | Flat (vocal); Sectioned (iam) | Yes (vocal); No (iam) |
| Dramas | Dramas, DramaticBibleReadings, gnj, VODMoviesBibleTimes, VODMoviesModernDay, VODMoviesAnimated, VODMoviesExtras | MediatorSectioned (Dramas, DramaticBibleReadings, VOD*); Flat (gnj). VOD* + gnj = MP4. | Yes |
| Books | wcg, lff, rr, lvs, lfb, yb17, yb16, bhs, jy, kr, ia, mb, jr, bt, lv, cf, jd, bh, my, lr, cl, fy, gt | Flat (MP3, GETPUBMEDIALINKS pub=code, no track param) | Yes |
| Yearbooks | yb15, yb14, yb13, yb12, yb11, yb10 | Flat (MP3, GETPUBMEDIALINKS pub=code, no track param) | Yes |
| Broadcasting | StudioMonthlyPrograms, StudioTalks, StudioNewsReports | MediatorSectioned (video) | Yes |
| BrochuresAndBooklets | lmd, wfg, lffi, th, rj, ypq, hf, jl, yc, hl, fg, ll, lc, lf, la, we | Flat (MP3, GETPUBMEDIALINKS pub=code, no track param) | Yes |
| Children | SeriesBJFLessons, BJF, ChildrenSongs, ChildrenMovies | MediatorSectioned (video MP4) | Yes |
| Family | FamilyChallenges, FamilyDatingMarriage, FamilyWorship, FamilyMovies | MediatorSectioned (video) | Yes |
| InterviewsAndExperiences | VODIntExpTransformations, VODIntExpBlessings, VODIntExpEndurance, VODIntExpYouth, OriginsLife, VODIntExpArchives | MediatorSectioned (video) | Yes |
| MeetingsAndMinistry | VODMinistryTools, VODMinistryImproveSkills, VODMinistryMethods, MeetingsConventions, VODSampleConversations | MediatorSectioned (video) | Yes |
| ProgramsAndEvents | VODPgmEvtMorningWorship, VODPgmEvtSpecial, VODPgmEvtGilead, VODPgmEvtAnnMtg, 2025Convention–2014Convention | MediatorSectioned (video) | Yes |
| Series | thv, VODLFFVideosAD, SeriesDigForTreasures, SeriesBJFSongs, … (display: "Video Series") | Flat (thv); MediatorSectioned (rest). SeriesDigForTreasures: 4 tracks, no sections. | Yes |
| Teenagers | TeenSpiritualGrowth, TeenSocialLife, TeenGoals, TeenWhatPeersSay, TeenMovies | MediatorSectioned (video) | Yes |
| Activities | VODActivitiesTranslation, VODActivitiesAVProduction, VODActivitiesPrintingShipping, VODActivitiesConstruction, VODActivitiesReliefWork, VODActivitiesTheoSchools, VODActivitiesSpecialEvents | MediatorSectioned (video) | Yes |
| Organization | Reports, VODOrgBethel, AccomplishMinistry, VODOrgHistory, VODOrgLegal, VODOrgBloodlessMedicine | MediatorSectioned (video) | Yes |
| FaithAndBible | BibleBooks, VODBibleReadingStudy, VODBibleTeachings, VODBibleAccounts, VODBibleMedia, VODBibleTranslations, VODBiblePrinciples, VODBibleCreation | MediatorSectioned (video) | Yes |
| ArticleSeries | mrt (More Topics), hdu (How Your Donations Are Used), lfs (Life Stories) | Flat (MP3, GETPUBMEDIALINKS pub=code, no track param) | Yes |

Notes:
- **Language-bound**: Yes = `BiblePublications.LanguageId != null`; No = no-language (e.g. iam).
- **Harvest type**: **Flat** = Publication → Tracks; fetch with GETPUBMEDIALINKS at pub level (no `track=` param; response contains all tracks). **Sectioned** = Publication → Sections → Tracks; fetch at section level (Bible: `booknum=`; iam: `pub=sectionCode`; no `track=`). **MediatorSectioned** = Use **mediator API only**: all track CDN URLs come from `category.media[].files[].progressiveDownloadURL`; do **not** call GETPUBMEDIALINKS for mediator publications. Similarly, when using GETPUBMEDIALINKS we do not call mediator for track URLs. **Track param is never passed**; we fetch at pub/section level and the response includes all tracks (with track numbers in the JSON).
- **Drama harvester** iterates **JwSourceHelper.AllMediatorPublicationCodes** (DramaCategoryCodes, VideoPublicationCodes, SeriesMediatorPublicationCodes, ChildrenMediatorPublicationCodes, BroadcastingPublicationCodes, TeenagersPublicationCodes, FamilyPublicationCodes, ProgramsAndEventsPublicationCodes, ActivitiesPublicationCodes, MeetingsAndMinistryPublicationCodes, OrganizationPublicationCodes, FaithAndBiblePublicationCodes, InterviewsAndExperiencesPublicationCodes).
- **Pre-harvested English (E)**: **JwSourceHelper.AllPublicationCodesForEnglishSeeding** = Bible ∪ VocalMusic ∪ DramaCategoryCodes ∪ VideoPublicationCodes ∪ Children ∪ Series ∪ Broadcasting ∪ Teenagers ∪ Family ∪ ProgramsAndEvents ∪ Activities ∪ MeetingsAndMinistry ∪ Organization ∪ FaithAndBible ∪ InterviewsAndExperiences ∪ ArticleSeries ∪ Books ∪ Yearbooks ∪ BrochuresAndBooklets (excludes melody-only iam). **EnglishSeeder** and E-seed validation use that set. **Flat audio (Article Series, Books, Yearbooks, Brochures and Booklets)** discovery: **MusicHarvester** methods HarvestArticleSeriesLinks, HarvestBooksLinks, HarvestYearbooksLinks, HarvestBrochuresAndBookletsLinks (same GETPUBMEDIALINKS flat-MP3 discovery as vocal music).
- **Category display names**: Series → "Video Series"; ArticleSeries → "Article Series" (in shared `Resources/CategoryNames/E.json`).
- **Category modal order**: Bible first, then rest alphabetically by category display name.

---

## Categories and languages schema (display names)

### Categories

- **Category** has **CategoryCode** only (e.g. `"Bible"`, `"Music"`). Display names come from embedded JSON resources in the shared project (`Resources/CategoryNames/{LanguageCode}.json`, e.g. `E.json` for English).
- **BiblePublication** is many-to-many with categories via **BiblePublicationCategories**. There is **no** `CategoryId` or `Category` navigation on `BiblePublication`.
- Use **PrimaryCategory** and **PrimaryCategoryId** (first junction entry) when a single “main” category is needed. When loading from DB, use `.Include(x => x.BiblePublicationCategories).ThenInclude(x => x.Category)`.
- Filter by category in queries: `x.BiblePublicationCategories.Any(bpc => bpc.Category.CategoryCode == "Music")` (not `x.Category.CategoryName`).

### Languages

- **Language** has **no** `Name` property. Display names are in **LanguageNamesByLanguage** (e.g. DisplayLanguageCode `"E"`).
- Resolve display names at runtime via **ILanguageNameService** (async `GetNameAsync` / `GetNamesAsync`, or sync **GetNameCached** / **GetNameByLanguageCodeCached** for use where async is awkward).

### In-memory caches (display names for "E")

- **ICategoryNameService** loads category code → display name from embedded JSON (e.g. `Resources/CategoryNames/E.json`) and warms an in-memory cache per language at startup. **ILanguageNameService** warms in-memory maps for `"E"` at startup (`BootstrapOrchestrator`).
- Use sync cache methods when calling from sync or fire-and-forget paths (e.g. **GetNameCached(int languageId)**, **GetNameByLanguageCodeCached(string languageCode)**).

---

When reasoning about lookups, the index supports multiple shapes:

- **publication code (always)**:
  - every content row hangs off a `BiblePublications.PublicationCode` (e.g. `nwt`, `bi12`, `iam`, `osg`)
- **language (optional)**:
  - most publications are language-bound (`BiblePublications.LanguageId != null`)
  - some are intentionally **no-language** (`LanguageId == null`) — notably melody discs like `iam`
  - app schedules may still carry a “current language” for UX, but media-index queries for no-language pubs should not require a language FK
- **sections (optional)**:
  - **sectioned** publications: Publication → Sections → Tracks (Bible, `iam` discs)
  - **non-sectioned** publications: Publication → Tracks (many vocals/videos)

### Track URL params (where they live)

**Only tracks store UrlParams** - complete params needed to fetch that specific track:
- `UrlParams.BiblePublicationTrackId` → `BiblePublicationTracks.Id`
- Each track's `UrlParams` are used to build the **refetch URL at publication or section level** (e.g. `pub`, `booknum`, `fileformat`, `langwritten`; we never pass `track=`). The API response contains all tracks; we match the desired track in the response by track number/code. Mediator publications get CDN URLs from the mediator response, not GETPUBMEDIALINKS.

**Sections and Publications do NOT store UrlParams**:
- Section URLs are built in code using harvest-type logic (derive from track params or use harvest-type rules)
- Publication URLs are built in code using harvest-type logic (see `FlatPublicationFetcher.cs`, `SectionFetcher.cs`)

The app should treat track `UrlParams` as **authoritative** and use them verbatim during playback/download via `UrlConstructionService`.

### HarvestType (where it lives and how it’s used)

**Where it lives**: `HarvestType` is on **BiblePublication** and on **PublicationLanguage** (discovery table) only. It is **not** on **BiblePublicationSection** or **SectionLanguage** — fetch strategy is per-publication (sectioned vs flat vs mediator), not per-section.

**How it’s decided**:
- **PublicationTypeHelper.GetHarvestType(publicationCode)** (in `JwSourceHelper` / `PublicationTypeHelper`): uses static lists (Bible → Sectioned, vocal/melody music → Sectioned/Flat, AllMediatorPublicationCodes → MediatorSectioned, video-only → Flat) to return the enum.
- **Harvester**: When creating publications, each builder sets it — e.g. **SectionFetcher** / **EnglishPublicationBuilder** → Sectioned, **FlatPublicationFetcher** → Flat, **DramaPublicationBuilder** → MediatorSectioned. **PublicationLanguage** gets HarvestType from seeders (e.g. **EnglishContentSeeder**, **PublicationLanguageSeeder**); **SectionLanguage** does not store HarvestType (decision comes from the publication).

**How it’s used during ad-hoc / normal fetch**:
- **PublicationEnsurer.EnsurePublicationExistsAsync**: If the **BiblePublication** already exists but **HarvestType** is null (e.g. old seed), it is **backfilled** from **PublicationTypeHelper.GetHarvestType**. When the publication does not exist, we load **PublicationLanguage** and use **publicationLanguage.HarvestType ?? GetHarvestType(code)** to choose the fetcher: **Sectioned** → fetch first section (SectionFetcher), **Flat** or **MediatorSectioned** → **FetchPublicationTracksAsync** (flat or drama).
- **LanguageContentPublicationTracksFetcher** / **EnglishContentSeeder**: Use **PublicationLanguage.HarvestType ?? GetHarvestType(code)** to decide Sectioned vs Flat vs MediatorSectioned and call the right fetch path.
- **SectionFetcher**: When creating a new publication/section, sets **HarvestType = Sectioned** on the **BiblePublication**; if an existing publication has **HarvestType == null**, it backfills to Sectioned.

Refetch/URL building uses the same rules (pub vs section level, mediator vs GETPUBMEDIALINKS) and does not need **HarvestType** on sections — the publication’s **HarvestType** or **PublicationLanguage.HarvestType** is enough.

### `iam` specifics (disc-style)

- Sections are disc codes like `iam-9`.
- Refetch/lookup is at section level: `pub=iam-9` (no `track=`); response contains all tracks. TrackCode comes from the JSON `track` field.
- Track titles for `iam` are seeded by the harvester (e.g. `Melody Number(s) 190, 172`) and the app should not add any prefixing/formatting on top.

---

## Where the index comes from (build-time)

### Harvester tool

Tool project:
- `.tools/Bible.Alarm.AudioLinksHarvestor/Program.cs`

It builds an index artifact at:
- `.tools/_index/index.zip` (see `.tools/Bible.Alarm.AudioLinksHarvestor/Utility/DirectoryHelper.cs`)

The MAUI app embeds that zip as an embedded resource:
- `src/Bible.Alarm/Bible.Alarm.csproj` embeds `..\..\.tools\_index\index.zip` as `Resources\index.zip`

### Harvester phases (important mental model)

The harvester runs in two big phases:

1) **DISCOVERY**
   - uses JW APIs to discover which languages exist for each publication/section
   - writes to in-memory stores, then later seeds “discovery tables”:
     - `PublicationLanguages`
     - `SectionLanguages`

2) **HARVESTING + SEEDING**
   - downloads/constructs the actual publication/section/track records that the app uses
   - seeds **English (“E”) by default**
   - seeds “no-language” melody music (e.g. `iam`) separately
   - zips the resulting SQLite DB into `index.zip`

---

## Harvest types (three modes)

Enum:
- `src/Bible.Alarm.Shared/Models/Enums/HarvestType.cs`

### 1) Flat

- publications: most **vocal music**, **video**
- shape: Publication → Tracks
- API: **GETPUBMEDIALINKS only** (no mediator for track URLs). One request per pub/language: `pub={code}&fileformat=MP3|MP4&langwritten={lang}` (no `track=`). Response contains all tracks; track numbers come from JSON `track` field.

### 2) Sectioned

- publications: **Bible** (books/parts), **melody music** (e.g. `iam` discs/sections)
- shape: Publication → Sections → Tracks
- API: **GETPUBMEDIALINKS only**. One request per section: Bible-style `pub={publicationCode}&booknum={sectionCode}`; melody disc-style `pub={sectionCode}`. No `track=` param; response contains all tracks for that section.

### 3) MediatorSectioned

- publications: all in **JwSourceHelper.AllMediatorPublicationCodes**: **Dramas** (Dramas, DramaticBibleReadings, VODMovies*), **Video** (gnj, VODMovies*), **Series** (VODLFFVideosAD, SeriesDigForTreasures, Series*, VODLovePeople, etc.), **Children**, **Broadcasting**, **Teenagers**, **Family**, **Programs and Events**, **Activities**, **Meetings and Ministry**, **Organization**, **Faith and Bible**, **Interviews and Experiences**.
- shape: **Mediator API only** for track CDN URLs. Single mediator response `GET /categories/{lang}/{categoryKey}` contains all tracks: `category.media[].files[].progressiveDownloadURL` is the CDN URL; `naturalKey` gives TrackCode (e.g. `pub-iaoh_E_12_AUDIO` → `iaoh-12`). Do **not** call GETPUBMEDIALINKS for mediator publications. When using GETPUBMEDIALINKS for other pubs we do not call mediator. **Track param is never passed** anywhere.

---

## Default seeded language and how others appear

### Default seeded language

English is the default seeded language:
- `AppConstants.Media.DefaultLanguageCode = "E"` (`src/Bible.Alarm.Shared/Constants/AppConstants.cs`)
- Harvester seeds English via:
  - `.tools/Bible.Alarm.AudioLinksHarvestor/Seeders/EnglishSeeder.cs`
  - `ILanguageContentService.SeedEnglishPublicationAsync(publicationCode)`

### Language discovery (catalog of what exists)

Harvester discovery saves:
- per-publication availability in `PublicationLanguages`
- per-section availability (Bible / sectioned content) in `SectionLanguages`

Those tables are intentionally separate from the “actual content” tables; they let the app answer:
- “does publication X exist in language Y?”
- “does section Z exist in language Y?”

### Ad-hoc download (runtime)

When the user picks a language/publication that isn’t pre-seeded:
- the app consults discovery tables to confirm availability
- then calls the shared media fetchers through:
  - `ILanguageContentService.EnsurePublicationExistsAsync(publicationCode, languageCode)`

This is used heavily by the **schedule cascade auto-population** when a language/category is selected and the publication hasn’t been harvested yet:
- `CategorySelectionAutoPopulateHandler` and `BiblePublicationCascadeHandler`

Important nuance (cascade relies on this):
- `EnsurePublicationExistsAsync(...)` consults `PublicationLanguages` for `HarvestType` and then fetches:
  - **Flat**: publication tracks
  - **Sectioned**: only the **first section + its tracks** (fast bootstrap for defaults), using `SectionLanguages` to find the “first section”
- Selecting a different section later can trigger additional track fetches (tracks are not guaranteed to exist just because sections exist).

### EF Core note: ordering “first section”

The app uses a **natural section-code comparer** (`SectionCodeHelper.SectionCodeComparer`) for UI-friendly ordering (numeric section codes sort numerically).

EF Core **cannot translate** `OrderBy(..., comparer)` into SQL, so code that needs the “first section” must:
- query candidate `SectionLanguages.SectionCode` values from SQLite first (e.g., `ToListAsync`)
- then apply `SectionCodeHelper.SectionCodeComparer` **in-memory**

See: `PublicationEnsurer.FetchFirstSectionWithTracksAsync(...)`.

For the full “selection cascade ↔ tables ↔ ad-hoc fetch” story, see:
- `schedule-cascade-deep-dive.mdc`

---

## APIs used (and what for)

### GETPUBMEDIALINKS (primary)

Constant:
- `AppConstants.ApiEndpoints.JwOrgIndexServiceBaseUrl = "https://b.jw-cdn.org/apis/pub-media/GETPUBMEDIALINKS"`

Used for:
- Bible audio links
- vocal music links
- melody music links
- video links
- drama links (after section discovery)

Common query params (varies by harvest type):
- `output=json`
- `pub={publicationCode}`
- `fileformat=mp3|mp4`
- `langwritten={LanguageCode}` (e.g. `E`)
- `alllangs=1` during discovery
- section selection for sectioned publications:
  - Bible: `booknum={sectionCode}`
  - Melody discs (e.g. `iam-9`): `pub={sectionCode}` (disc code becomes the pub)

### Mediator API (category discovery)

Constants:
- `AppConstants.ApiEndpoints.JwOrgMediatorApiBaseUrl` – primary (app.jw-cdn.org)
- `AppConstants.ApiEndpoints.JwOrgMediatorApiBaseUrls` – `["https://b.jw-cdn.org/apis/mediator/v1", "https://app.jw-cdn.org/apis/mediator/v1"]` for random pick / retry

Use `GetPubMediaLinksRetry.GetStringAsync(httpClient, JwOrgMediatorApiBaseUrls, pathAndQuery)` (or harvester `DownloadUtility.GetMediatorAsync(pathAndQuery)`) so requests try each base in random order.

Used mainly for:
- drama section discovery (and in some cases localized metadata)

### Languages list (seed language table)

Harvester seeds the full language list using the JW site language endpoint:
- implemented in `.tools/Bible.Alarm.AudioLinksHarvestor/Seeders/LanguageSeeder.cs` (called from `DbSeeder`)

---

## Sample API Links for Track CDN URLs

**Base URL**: `https://b.jw-cdn.org/apis/pub-media/GETPUBMEDIALINKS`

We **never pass `track=`**. Requests are at **publication** or **section** level; the API response contains all tracks (each with a `track` field in the JSON). LookUpPath / refetch URLs are built the same way.

### Bible Publications (bi12, nwt, osg, pksjj)

**Format** (section-level; no `track=`): `?output=json&pub={pubCode}&booknum={bookNumber}&fileformat=MP3&alllangs=0&langwritten={langCode}`

Examples:
- **nwt** Book 1 (all tracks): `?output=json&pub=nwt&booknum=1&fileformat=MP3&alllangs=0&langwritten=E`
- **bi12** Book 1 (all tracks): `?output=json&pub=bi12&booknum=1&fileformat=MP3&alllangs=0&langwritten=E`

**TrackCode Source**: `trackFile["track"]` from JSON response (e.g., `"1"`, `"2"`). Match track in response by track number.

### iam (Kingdom Melodies)

**Format** (section-level; no `track=`): `?output=json&pub={discCode}&fileformat=MP3&alllangs=0&langwritten=E`

Examples:
- Disc 1 (all tracks): `?output=json&pub=iam-1&fileformat=MP3&alllangs=0&langwritten=E`
- Disc 2 (all tracks): `?output=json&pub=iam-2&fileformat=MP3&alllangs=0&langwritten=E`

**TrackCode Source**: `trackFile["track"]` from JSON response (per disc). Match track in response by track number.

### Dramas (Mediator-discovered: Dramas, DramaticBibleReadings, VOD*, SeriesDigForTreasures, etc.)

**Mediator only**: Do **not** call GETPUBMEDIALINKS for mediator publications. All track CDN URLs come from the **mediator API** response: `GET /apis/mediator/v1/categories/{lang}/{categoryKey}`. Each track: `category.media[].files[].progressiveDownloadURL` is the CDN URL; `naturalKey` (e.g. `pub-iaoh_E_12_AUDIO`) yields TrackCode (e.g. `iaoh-12`). No per-track GETPUBMEDIALINKS requests.

### Music Publications (sjjc, sjji, snv)

**Format** (pub-level; no `track=`): `?output=json&pub={pubCode}&fileformat=MP3&alllangs=0&langwritten={langCode}`

Examples:
- **sjjc** (all tracks): `?output=json&pub=sjjc&fileformat=MP3&alllangs=0&langwritten=E`
- **sjji** (all tracks): `?output=json&pub=sjji&fileformat=MP3&alllangs=0&langwritten=E`
- **snv** (all tracks): `?output=json&pub=snv&fileformat=MP3&alllangs=0&langwritten=E`

**TrackCode Source**: `track` from JSON response. Match track in response by track number.

### Video Publications (gnj)

**Format** (pub-level; no `track=`): `?output=json&pub={pubCode}&fileformat=MP4&alllangs=0&langwritten={langCode}`

Example:
- **gnj** (all episodes): `?output=json&pub=gnj&fileformat=MP4&alllangs=0&langwritten=E`

**Note**: Uses `MP4` format instead of `MP3`. Response contains all episodes; track numbers from JSON or sequential (1, 2, 3...).

### Harvest validation (sample vs API)

After seeding, the harvester runs **HarvestValidator** (`.tools/Bible.Alarm.AudioLinksHarvestor/Utility/HarvestValidator.cs`): it loads sample tracks from the DB (nwt, bi12, sjjc, osg, iam, gnj, thv, Dramas, SeriesDigForTreasures, VODMoviesBibleTimes, etc.). For GETPUBMEDIALINKS publications it builds the URL at **section or publication level** (no `track=`) from **UrlParams**, fetches the API, and compares. For mediator publications validation may use mediator or stored CDN. It compares:
- **Publication name**: API `pubName` vs DB `BiblePublication.Name`. For **sectioned/mediator** publications the API often returns the **section or episode title** (e.g. "Genesis", "Kingdom Melodies, Volume 1", "JW Broadcasting") rather than the publication name; the validator treats title match as pass for these.
- **Track title**: API first track `title` vs DB `BiblePublicationTrack.Title`.

So a “pass” means the stored URL params and track title align with the live API; pubName differences for sectioned/mediator are expected and not treated as failure.

### Response Format

All endpoints return JSON:
```json
{
  "pubName": "Publication Name",
  "files": {
    "E": {
      "MP3": [
        {
          "file": {
            "url": "https://cdn.jwplayer.com/videos/...",
            "duration": 180
          },
          "track": 1,
          "title": "Track Title"
        }
      ]
    }
  }
}
```

**For Dramas** (no track property):
```json
{
  "pubName": "Drama Title",
  "files": {
    "E": {
      "MP3": [
        {
          "file": {
            "url": "https://cdn.jwplayer.com/videos/...",
            "duration": 3600
          },
          "title": "Drama Title"
        }
      ]
    }
  }
}
```

The CDN URL is in `files.{lang}.MP3[0].file.url` (or `MP4` for videos).

---

## How the app gets the index at runtime

On first run (or if missing), the app extracts the embedded resource:
- `src/Bible.Alarm/Services/Media/MediaIndexService.cs`
  - copies embedded `index.zip` into storage root
  - extracts to produce `mediaIndex.db`
  - writes a version stamp via `IMediaIndexVersionService`

Notes:
- The code currently assumes the media index is updated by **app updates**, not by downloading a new index from the network (see comments in `MediaIndexService`).

---

## Non-English media data migration on version change

The packaged `mediaIndex.db` only contains English ("E") and no-language publications. Non-English content is fetched ad-hoc at runtime when users select other languages. On app version change, the media index is replaced with the new packaged version, which would lose that ad-hoc data.

### Migration flow (runs during bootstrap, before schedule population)

Location: `src/Bible.Alarm/Services/Media/MediaIndexService.cs` → `MigrateNonEnglishDataIfNeededAsync()`
Helpers: `src/Bible.Alarm/Services/Media/MediaIndexServiceHelpers/`

1. `MediaIndexService.ClearCopyIndexFromResource()` **renames** the old `mediaIndex.db` to `mediaIndex_old.db` (instead of deleting it), then extracts the new packaged index.
2. After both database and resource bootstrap complete, `MigrateNonEnglishDataIfNeededAsync()` runs:
   - `NonEnglishMediaDataMigrator` reads all non-English `(PublicationCode, LanguageCode, SectionCode)` references from `BiblePublicationSchedules` and `AlarmMusic` in the Schedule DB.
   - For each unique `(PublicationCode, LanguageCode)`, copies from old → new media index:
     - `BiblePublication` row (mapped by `CategoryName` and `LanguageCode`, not by integer IDs)
     - For sectioned pubs: the specific `BiblePublicationSection` row(s) referenced by schedules + their tracks
     - For flat pubs: all tracks (where `BiblePublicationSectionId IS NULL`)
     - `UrlParams` for each track (with `ApiUrl` ID remapping via `Url` value)
   - Uses raw SQL with `ATTACH DATABASE` (not EF Core) — safe across schema/migration version mismatches.
   - Each publication is migrated in its own transaction; failures are logged and skipped.
3. `OrphanedScheduleCleanup` verifies all non-English schedule references exist in the new media index:
   - If a `BiblePublicationSchedule`'s publication can't be found → **deletes the entire alarm schedule** (prevents home page display failures).
   - If an `AlarmMusic` publication can't be found → **resets music** (disables `MusicEnabled`, deletes `AlarmMusic` row; schedule is preserved).

### Ordering guarantee

This runs in `BootstrapOrchestrator` after `Task.WhenAll(database, resources, fluxor)` completes but **before** `scheduleBootstrapService.InitializeAsync()` (home page population) and before Android Auto can process play clicks.

### Key design decisions

- **Maps by codes, not IDs**: PKs/FKs differ between old and new DBs. All cross-DB mapping uses stable identifiers (`PublicationCode`, `LanguageCode`, `SectionCode`, `TrackCode`, `CategoryName`, `ApiUrl.Url`).
- **Deduplication**: References are grouped by `(PublicationCode, LanguageCode)` with section codes deduplicated via `.Distinct()`. An existence check prevents re-inserting publications already in the new index.
- **Graceful degradation**: Migration failures are non-fatal per publication. The orphaned schedule cleanup acts as a safety net, deleting any schedules that can't resolve their media data.

